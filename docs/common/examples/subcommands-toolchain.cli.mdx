---
climat_version: 0.0.5
---
```clidsl title="toolchain.cli"
my-toolchain {
  act sh { echo 'Hello' }

  sub child-toolchain {   // <- Child name
    act sh { echo Child } // <- Child action
  }
}
```